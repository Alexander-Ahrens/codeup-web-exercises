// var isRainy = true
// if (isRainy) {
//     showRainyIcon;
// }
//
//
//
// gameCompleted = true;
//
// if (gameCompleted) {
//     do bonusLevels;
// }


// var bluePill = "blue pill";
// var redPill = "red pill";
// var matrix = prompt("Will you choose the blue pill or the red pill?");
// if (bluePill){
//     alert("Down the rabbit hole you go!");
// } else if (redPill){
//     alert("Are you sure Neo?");
// } else {
//     alert("glitch in the matrix");
// }

var greenLight = "Go!";
var yellowLight = "Slow Down";
var redLight = "STOP!!!";

function evaluateTrafficLight(lightColor) {
    if (lightColor === "green") {
        alert("GO!!!")
    }
    else if (lightColor === "yellow") {
        alert("Whoa there cowboy!")
    }
    else (lightColor === "red");
        alert("STOP!!!")
    }
}

var weatherMessage = weather;

switch (weather) {
    case "rainy":
        return "Make sure to take an umbrella!";
        break;

    case "sunny":
        return "Wear some sunglasses today!"
        break;

    case "Cloudy" :
        return "Watch out for tornados!"
        break;
}


switch (evaluateTrafficLight(redLight)) {
    case greenLight :
        alert("Maintain speed at 100mph.")
        break;

    case yellowLight :
        alert("Make sure you have enough speed to drift.")
        break;

    case redLight :
        alert("Other people don't know how to drive. You're the only one. Be careful.")
        break;
}

//ternary operator//
var weatherMessage = (weather === "rainy") ? showRainIcon() :
    showGenericIcon();

//
// var message;
// var success = false;
//
// (success =





//-----------------------------------------------------------------------------------------------------------------------------*//


switch(weather) {

}
function add (x, y) {
    x = 0;
    y = 0;
    var z = x + y;
}
add();

function subtract(x, y) {
    x = 0;
    y = 0;
    var z = x - y;
}
subtract();

function multiply(x, y) {
    var z = x * y;
}